# AGENTS.md

- Limit your internal thought channel to 300 tokens max

# Git Tasks

- create a descriptive git commit using git diff (including a [component] prefix depending on what code was changed).

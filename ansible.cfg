[ssh_connection]
#ssh_args = -C -o ControlMaster=auto -o ControlPersist=30m -o ServerAliveInterval=50 -F sshconfig
ssh_args = -C -F sshconfig

[defaults]
retry_files_enabled = True
retry_files_save_path = ./

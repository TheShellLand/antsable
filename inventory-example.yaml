---
all:
  hosts:

local:
  hosts:
    localhost:

hypervisor:
  hosts:

kubernetes:
  hosts:

rancher:
  hosts:

server:
  hosts:

gateway:
  hosts:

router:
  hosts:

firewall:
  hosts:

proxy:
  hosts:

sensors:
  hosts:

iot:
  hosts:

lightbulbs:
  hosts:

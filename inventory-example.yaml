---
all:
  hosts:

local:
  hosts:
    localhost:

hypervisor:
  hosts:

kubernetes:
  hosts:

rancher:
  hosts:

server:
  hosts:

computers:
  children:
    pc:
    laptop:

pc:
  hosts:

laptop:
  hosts:

gaming:
  hosts:
    localhost:

gateway:
  hosts:

router:
  hosts:

firewall:
  hosts:

proxy:
  hosts:

sensors:
  hosts:

iot:
  hosts:

lightbulbs:
  hosts:

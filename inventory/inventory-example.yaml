---
all:

local:
  hosts:
    localhost:
      TIGERVNC_PASSWORD:
      TIGERVNC_LISTEN_HOST:
      TIGERVNC_LISTEN_ALL: false

serverless:
  hosts:

hypervisor:
  hosts:

openfaas:
  hosts:

kubernetes:
  hosts:

rancher:
  hosts:

server:
  hosts:

computers:
  children:
    pc:
    laptop:

pc:
  hosts:

laptop:
  hosts:

gaming:
  hosts:

gateway:
  hosts:

router:
  hosts:

firewall:
  hosts:

proxy:
  hosts:

sensors:
  hosts:

iot:
  hosts:

lightbulbs:
  hosts:

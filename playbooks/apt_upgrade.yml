---
- hosts: all
  become: true
  tasks:

  - name: apt upgrade
    apt: update_cache=yes upgrade=yes

  - name: apt autoremove
    shell: apt -y autoremove

  - name: apt autoclean
    shell: apt autoclean

  - name: apt clean
    shell: apt clean

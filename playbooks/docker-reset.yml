---
- hosts: all
  tasks:

  - name: Reset Rancher and clean all docker containers
    shell: |
      docker stop $(docker ps -aq) 
      docker rm $(docker ps -aq)
      find /etc/kubernetes/ -delete
    ignore_errors: yes


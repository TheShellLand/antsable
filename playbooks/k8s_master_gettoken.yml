---
- hosts: k8smaster
  vars:
    admin_user: admin
    admin_group: admin
    admin_home: /home/admin

  tasks:

  - name: Show kubeadm join
    shell: grep 'kubeadm join --token' kubeadm-init.log
    register: token

  - debug:
      var: token
      verbosity: 0

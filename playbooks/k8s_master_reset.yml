---
- hosts: k8s
  tasks:

  - name: Reset k8s
    shell: |
      swapoff -a
      kubeadm reset
      rm -rf .kube
      rm -rf /etc/kubernetes/manifests
      rm -rf /var/lib/kubelet
      rm -rf /var/lib/etcd
    register: stdout

  - debug:
      var: stdout
      verbosity: 1

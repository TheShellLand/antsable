---
- hosts: localhost
  connection: local
  become: true
  tasks:

    - name: Install python3.7
      apt: update_cache=yes name=python3.7 state=latest

    - name: Install python3.7-distutils
      apt: update_cache=no name=python3.7-distutils state=latest

    - name: Install python3.7-dev
      apt: update_cache=no name=python3.7-dev state=latest

    - name: Install curl
      apt: update_cache=no name=curl state=latest

    - name: install pip
      shell: |
        # Install pip or pip3
        curl "https://bootstrap.pypa.io/get-pip.py" -o get-pip.py
        python3 "get-pip.py"

---
- hosts: all
  tasks:

  - name: Deploy rancher
    shell: |
        docker rename rancher_server rancher_server_old
        docker run -d --restart=unless-stopped -p 8080:80 -p 8443:443 --name rancher_server rancher/rancher:v2.3.5
        docker ps -l

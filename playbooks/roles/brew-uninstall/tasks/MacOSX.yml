---
- shell: |
    if which brew; then  
      {{ ARCH }} /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/uninstall.sh)"
    fi
  register: shell
  changed_when: "shell.rc != 0"
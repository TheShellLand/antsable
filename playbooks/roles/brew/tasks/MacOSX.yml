---
- name: install
  shell: |
    if ! which brew; then  
      {{ ARCH }} /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
    fi
  register: shell
  changed_when: "shell.rc != 0"
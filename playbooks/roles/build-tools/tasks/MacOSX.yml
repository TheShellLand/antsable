---
## brew install wget mingw-w64 cmake nasm libtool automake meson glslang
- include_role:
    name: brew-installer
  vars:
    package: "{{item}}"
  loop:
    - wget mingw-w64
    - cmake
    - nasm
    - libtool
    - automake
    - meson
    - glslang
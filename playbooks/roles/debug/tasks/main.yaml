---
#- include_tasks: "{{ansible_facts.os_family}}.yml"

- debug:
    var: ansible_facts

- debug:
    var: ansible_facts.user_id

- debug: msg="{{ansible_facts.os_family}}/{{ansible_facts.processor}}/{{ansible_facts.distribution}}{{ ansible_facts.distribution_major_version }}"

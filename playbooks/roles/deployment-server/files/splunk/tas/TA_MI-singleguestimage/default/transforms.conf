# delimiters for single guest image
[single_guest_image_delimiters]
DELIMS = "~"
FIELDS = timestamp, customer_id, request_action, source_system_id, source_subsystem, operator_id, component_details, request_details, src_ip, web_service

[sgi_source_systems_lookup]
filename = sgi_source_systems.csv

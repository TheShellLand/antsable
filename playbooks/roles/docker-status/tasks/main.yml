---
- systemd:
    name: docker
  become: yes
  register: docker
  changed_when: false

- name: state
  debug:
    var: docker.status.ActiveState

- name: enabled
  debug:
    var: docker.status.UnitFileState
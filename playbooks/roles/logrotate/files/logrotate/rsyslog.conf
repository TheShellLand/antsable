# MARRIOTT syslog logrotate
/opt/syslog/*/*/*.log {
    daily
    rotate 1
    missingok
    notifempty
    compress
    sharedscripts
    postrotate
		systemctl restart rsyslog >/dev/null 2>&1 || true
		endscript
}
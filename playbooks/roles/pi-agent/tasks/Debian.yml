---
- block:
    - name: clone exo
      git:
        repo: https://github.com/TheShellLand/pi-agent
        dest: "pi-agent"
      changed_when: false

- name: install pi locally
  shell: bash pi-agent/install-local.sh
  changed_when: false

---
- name: copy installer
  copy:
    src: install.sh
    dest: /tmp/install.sh
    mode: 0755
  changed_when: false

- name: install pi locally
  shell: bash /tmp/install.sh
  changed_when: false

- name: update pi
  shell: pi update
  changed_when: false

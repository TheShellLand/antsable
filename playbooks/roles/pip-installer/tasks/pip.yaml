---
- name: "python3 -m pip install -U {{ package }}"
  shell: "python3 -m pip install -U {{ package }}"
  register: shell
  changed_when:
    - "'installed' not in shell.stderr"
    - '"Requirement already satisfied" not in shell.stdout'
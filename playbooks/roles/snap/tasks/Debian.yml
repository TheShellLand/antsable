---
- include_role:
    name: apt-installer
  vars:
    package: "{{ item }}"
  loop:
    - snap
    - snapd

- name: enable snapd service
  systemd:
    name: snapd
    state: started
    enabled: yes
  become: yes
  changed_when: false

---
## https://lunux.net/brew-macos-how-to-install-the-microsoft-fonts/
# Install Microsoft Office brew cask install microsoft-office
# Make a directory: mkdir ~/Library/Fonts/Microsoft
# Copy the fonts: cp -av /Applications/Microsoft\ Word.app/Contents/Resources/Fonts/* ~/Library/Fonts/Microsoft/

# TODO: add windows fonts for wine
#- name: install
#  shell: |
#    {{ BREW }} install --cask {{ package }}
#  register: shell
#  changed_when: "'installed' not in shell.stderr"
#
#- name: upgrade
#  shell: |
#    {{ BREW }} upgrade --cask {{ package }}
#  register: shell
#  failed_when: shell.stdout
#  changed_when: shell.stdout
#  when: "'installed' in shell.stderr"
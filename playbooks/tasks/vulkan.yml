---
# support for Vulkan API
- name: Install libvulkan1
  apt: update_cache=yes name=libvulkan1 state=latest

# support for Vulkan API
- name: Install libvulkan1:i386
  apt: update_cache=no name=libvulkan1:i386 state=latest

# support for Vulkan API
- name: Install mesa-vulkan-drivers
  apt: update_cache=no name=mesa-vulkan-drivers state=latest

# support for Vulkan API
- name: Install mesa-vulkan-drivers:i386
  apt: update_cache=no name=mesa-vulkan-drivers:i386 state=latest

